spring:
  application:
    name: sys-service
  datasource:
    url: jdbc:mysql://${MYSQL_SERVER}/dms
    username: root
    password: root
  cloud:
    loadbalancer:
      nacos:
        enabled: true
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER}

server:
  port: 0
